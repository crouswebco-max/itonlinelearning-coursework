// Changes the heading text when the button is clicked
function changeHeading() {
    // Find the heading by its id and store it in a variable
    let heading = document.getElementById("main-heading");

    // Give the heading new text
    heading.textContent = "The heading was changed by JavaScript!";
}
